# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# C extensions
*.egg-info/
.installed.cfg
*.egg

# Testing
.pytest_cache/

# Virtual environments
.env
.envrc
.venv
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/

# Data & logs
data/*
data/.*
!data/genotypes/
!data/genotypes/*.json
logs/
skills/dynamic-skills/

# Docker
*.log
.dockerignore

# Models cache
models/
.cache/
tendril-node-*.pub

# CLI build artifacts
cli/dist/
cli/tendril-cli

# Go Stem build artifacts
cmd/stem/stem
cmd/stem/tendril
cmd/stem/tendril-cli
cmd/stem/server.log
cmd/stem/dist/

# Release build output (reproducible via `make release` / release.yml)
dist/

# Generated .tendril runtime files (user-local, not committed)
.tendril/transduction/hormonal-triggers/block-trigger.sh
.tendril/transduction/hormonal-triggers/*-generated.sh

# Transient AI Agent Artifacts & Workspaces (Never commit these)
.ai/
.scratch/
scratch/
scratch.md
task.md
walkthrough.md
implementation_plan.md
.aider*
.cline*
.tendril/dreamer.db
.tendril/dreamer.key
.tendril/terrarium/
substrates.yaml

# Compiled stem binary in root (use cmd/stem/stem path)
/stem

# Runtime tendril data (cryptographic keys, DBs, runtime scoring)
.tendril/rhizome.db
.tendril/rhizome.key
.tendril/dreamer.db
.tendril/dreamer.key
.tendril/genome/fitness.json
.tendril/

# Private keys / secrets (e.g. GitHub App .pem) — never commit
*.pem
*.private-key.pem
